[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "python3.11 -m pip install --upgrade pip",
  "pip3 install -r requirements.txt"
]

[start]
cmd = "gunicorn student_management_system.wsgi:application"